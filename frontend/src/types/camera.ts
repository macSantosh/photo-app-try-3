import { Camera, CameraCapturedPicture, CameraType } from 'expo-camera';
import type { ComponentProps } from 'react';

export type { CameraCapturedPicture, CameraType };

// Get the component props type from the Camera component
export { Camera };
